#!/bin/bash
exec > /var/log/deploy_homepage.log 2>&1

echo "===== Deploying RED/ORANGE Homepage: SERVER C ====="

# Install Nginx if not present
yum install -y nginx || apt-get update && apt-get install -y nginx

systemctl enable nginx
systemctl start nginx

cat <<'EOF' > /usr/share/nginx/html/index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Server C - DevOPS Academy (RED/ORANGE)</title>
  <style>
    body {
      background: linear-gradient(120deg, #ff512f, #dd2476, #ff8c00, #f7971e);
      background-size: 350% 350%;
      animation: gradientBG 10s ease infinite;
      min-height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
    }
    @keyframes gradientBG {
      0% {background-position: 0% 50%;}
      50% {background-position: 100% 50%;}
      100% {background-position: 0% 50%;}
    }
    .container {
      background: rgba(255,255,255,0.17);
      box-shadow: 0 8px 32px 0 rgba(245,100,30,0.37);
      border-radius: 32px;
      padding: 45px 65px;
      text-align: center;
      color: #fff;
      border: 1px solid rgba(255,255,255,0.20);
      backdrop-filter: blur(8px);
      animation: fadeIn 2s;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(50px);}
      to { opacity: 1; transform: translateY(0);}
    }
    h1 {
      font-size: 2.7em;
      margin-bottom: 0.5em;
      letter-spacing: 2px;
      color: #ff8c00;
      text-shadow: 0 2px 8px rgba(255,81,47,0.18);
    }
    .ami-label {
      margin: 2em 0;
      display: inline-block;
      padding: 0.6em 2.3em;
      border-radius: 18px;
      background: #fff;
      color: #ff512f;
      font-size: 1.1em;
      font-weight: bold;
      border: 2px dashed #ff512f;
      box-shadow: 0 2px 8px rgba(255,81,47,0.09);
      letter-spacing: 1px;
    }
    .btn {
      margin-top: 2em;
      padding: 0.8em 2.4em;
      border: none;
      border-radius: 50px;
      background: #ff512f;
      color: #fff;
      font-size: 1.15em;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 2px 8px rgba(255,81,47,0.12);
      transition: all 0.2s;
      letter-spacing: 1px;
      outline: none;
    }
    .btn:hover {
      background: #f7971e;
      color: #ff512f;
      transform: scale(1.06) rotate(-2deg);
    }
    .footer {
      margin-top: 3em;
      font-size: 1em;
      color: #ffe6d2;
      letter-spacing: 2px;
      opacity: 0.9;
      border-top: 1px solid #ffffff18;
      padding-top: 1.2em;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🔥 This is SERVER C [Red/Orange]<br>Welcome to DevOPS Academy!</h1>
    <p>
      Served from <b>EC2 instance C</b> behind your load balancer.<br>
      <span style="font-size:1.15em;">You're seeing the RED/ORANGE version.<br>
      Canary deployments, anyone? 🚦</span>
    </p>
    <div class="ami-label">
      Powered by <b>personalized Nginx AMI</b><br>
      <i>Auto-deployed for modern DevOps experiments</i>
    </div>
    <button class="btn" onclick="alert('Red Server C: Your canary is alive!')">Try Me!</button>
    <div class="footer">
      — DevOPS Academy &copy; 2025 —
    </div>
  </div>
</body>
</html>
EOF

echo "Homepage deployed: SERVER C (RED/ORANGE)"
